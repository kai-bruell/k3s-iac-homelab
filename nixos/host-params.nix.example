# Diese Datei wird von Terraform generiert (templatefile).
# Nicht manuell editieren! Siehe: terraform/modules/nixos-vm/templates/host-params.nix.tftpl
#
# Zum lokalen Testen: Kopiere diese Datei nach host-params.nix und passe die Werte an.
{ config, pkgs, lib, ... }:

{
  networking.hostName = "ollama";

  networking.useDHCP = false;
  networking.interfaces.ens18 = {
    ipv4.addresses = [{
      address = "192.168.178.60";
      prefixLength = 24;
    }];
  };
  networking.defaultGateway = "192.168.178.1";
  networking.nameservers = [ "192.168.178.1" ];

  users.users.root.openssh.authorizedKeys.keys = [
    "ssh-ed25519 AAAA... user@host"
  ];
}
